<form method="post" action="{{ path('app_clanky_delete', {'id': clanky.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ clanky.id) }}">
    <button class="btn">Delete</button>
</form>
